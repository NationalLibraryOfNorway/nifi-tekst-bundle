*.jp2 filter=lfs diff=lfs merge=lfs -text
# Ensure all other XML files are treated as normal text
*.xml !filter !diff !merge text
# Track specific XML files related to OCR with LFS
**/ocr/*.xml filter=lfs diff=lfs merge=lfs -text
**/technical/jhove/*.xml filter=lfs diff=lfs merge=lfs -text