# Git LFS
*.jp2 filter=lfs diff=lfs merge=lfs -text
*.jp2.xml filter=lfs diff=lfs merge=lfs -text

# XML defaults to normal text (must be AFTER the LFS exceptions above if you use negative rules)
*.xml text

# Specific XML directories that should be LFS
**/ocr/*.xml filter=lfs diff=lfs merge=lfs -text
**/technical/jhove/*.xml filter=lfs diff=lfs merge=lfs -text
