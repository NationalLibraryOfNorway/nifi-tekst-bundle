[project]
name = "nifi-tekst-python"
version = "2.0.0"
requires-python = ">=3.9"
dependencies = [
    "opencv-python-headless",
    "numpy",
    "Pillow",
]

[dependency-groups]
test = [
    "pytest",
]
